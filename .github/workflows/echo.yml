name: Echo Test
on: [push, pull_request]
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Say Hello
        run: echo "🚀 GitHub Actions is working!"
